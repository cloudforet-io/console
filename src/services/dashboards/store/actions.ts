import type { Action } from 'vuex';

import type { DashboardsState } from '@/services/dashboards/store/type';

export const setDashboardsList: Action<DashboardsState, any> = async (): Promise<void> => {
    // just
};
