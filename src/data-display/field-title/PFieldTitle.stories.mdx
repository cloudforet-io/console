import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs/blocks';

import PFieldTitle from './PFieldTitle.vue';

import { getLabelArgTypes } from '@/data-display/field-title/story-helper';


<Meta title='Data Display/Field Title' parameters={{
    design: {
        type: 'figma',
        url: 'https://www.figma.com/file/IS6P8y1Wn2nfBC4jGlSiya/Components?node-id=10%3A193199'
    }
}} component={PFieldTitle} argTypes={getLabelArgTypes()}
/>

export const Template = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { PFieldTitle },
    template: `
    <div class="h-full w-full overflow p-8">
        <p-field-title :label="label">
            <template v-if="defaultSlot"><span v-html="defaultSlot"/></template>
        </p-field-title>
    </div>
    `,
    setup() {
        return {}
    }
});


# Field Title
<br/>
<br/>


<Canvas>
    <Story name="Basic">
        {{
            components: { PFieldTitle },
            template: `
    <div class="h-full w-full overflow p-8">
        <p-field-title>Field Title</p-field-title>
    </div>
    `,
            setup() {
                return {}
            }
        }}
    </Story>
</Canvas>

<br/>
<br/>


<Canvas>
    <Story name="Playground">
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable story="Playground"/>
