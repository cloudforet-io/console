import {Meta, Canvas, Story, ArgsTable} from '@storybook/addon-docs/blocks';
import PBadge from "@/data-display/badge/PBadge";
import { BADGE_STYLE, BADGE_STYLE_OUTLINE } from '@/data-display/badge/type';
import { getBadgesArgTypes } from '@/data-display/badge/story-helper';

<Meta title='Data Display/Badge' parameters={{
    design: {
        type: 'figma',
        url: 'https://www.figma.com/file/wq4wSowBcADBuUrMEZLz6i/SpaceONE-Console-Design?node-id=6132%3A124077',
    }
}} argTypes={getBadgesArgTypes()}/>

export const Template = (args,{argTypes})=>({
    props: Object.keys(argTypes),
    components: { PBadge },
    template: `
        <div style="display:flex; align-items:center; justify-content:center; height:100px;">
            <p-badge :styleType="styleType"
                    :text-color="textColor"
                    :backgroundColor="backgroundColor"
                    :outline="outline"
                    :shape="shape"
            >{{$props.defaultSlot}}</p-badge>
        </div>`,
})

# Badge

<br/>
<br/>
<br/>

## Style Types

<Canvas>
    <Story name='Style Types'>
        {{
            components:{ PBadge },
            template: `
                <div class="w-full h-full flex flex-wrap items-center justify-center">
                    <p-badge v-for="styleType in styleTypes" :styleType="styleType" :key="styleType" class="m-2">
                        {{styleType}}
                    </p-badge>
                </div>`,
            setup() {
                return {
                    styleTypes: Object.values(BADGE_STYLE)
                }
            }
        }}
    </Story>
</Canvas>

## Outlined

<Canvas>
    <Story name='Outlined'>
        {{
            components:{ PBadge },
            template: `
                <div class="w-full h-full flex flex-wrap items-center justify-center">
                    <p-badge v-for="styleType in styleTypes" :styleType="styleType" outline class="m-2">
                        {{styleType}}
                    </p-badge>
                </div>`,
            setup() {
                return {
                    styleTypes: Object.values(BADGE_STYLE_OUTLINE)
                }
            }
        }}
    </Story>
</Canvas>

## Shapes

<Canvas>
    <Story name="Shapes" height="100px">
        {{
            components: {PBadge},
            template: `
                <div style="display: flex; align-items: center; justify-content: center; height: 100px">
                    <p-badge shape="round">
                        <div>round</div>
                    </p-badge>
                    <p-badge shape="square" style="margin-left: 20px">
                        <div>square</div>
                    </p-badge>
                </div>`
        }}
    </Story>
</Canvas>

## Playground

<Canvas>
    <Story name="Playground" height="100px">
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable story="Playground" />
