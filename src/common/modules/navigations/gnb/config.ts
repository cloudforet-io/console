import type { MenuId } from '@/lib/menu/config';
import { MENU_ID } from '@/lib/menu/config';

export const customMenuNameList: (MenuId | string)[] = [
    MENU_ID.DASHBOARDS,
    'Integrations',
];

