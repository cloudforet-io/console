export enum JOB_TASK_STATUS {
    pending = 'PENDING',
    progress = 'IN_PROGRESS',
    success = 'SUCCESS',
    failure = 'FAILURE',
    error = 'ERROR',
    timeout = 'TIMEOUT',
    canceled = 'CANCELED',
}

export enum JOB_STATUS {
    created = 'CREATED',
    canceled = 'CANCELED',
    progress = 'IN_PROGRESS',
    success = 'SUCCESS',
    error = 'ERROR',
    timeout = 'TIMEOUT',
}
