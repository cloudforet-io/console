# review for PRs
name: SpaceONE Review

on:
  pull_request:
    path-ignores:
      - '.github/**'
      - '**/deploy/helm/**'
      - '**/skaffold.template.yaml'
    branches:
      - master


jobs:
  review:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'
          cache: 'npm'

      - name: Cache dependencies
        uses: actions/cache@v2
        env:
          cache-name: cache-node-modules
        with:
          path: node_modules
          key: ${{ runner.os }}-node-modules-${{ hashFiles('package-lock.json') }}

      - name: Install dependencies
        if: steps.node-cache.outputs.cache-hit != 'true'
        run: npm install

      - name: Build
        run: |
          TURBO_TEAM=${{ secrets.TURBO_TEAM }} TURBO_TOKEN=${{ secrets.TURBO_TOKEN }} npm run build

      - name: Lint
        run: |
          npm run lint

      - name: Unit Test
        run: |
          npm run test

