{/* PSelectDropdown.mdx */}

import { Canvas, Meta, Controls } from '@storybook/blocks';

import * as PSelectDropdownStories from './PSelectDropdown.stories';

<Meta of={PSelectDropdownStories} />


# Select Dropdown
<br/>
<br/>

## Basic
<Canvas of={PSelectDropdownStories.Basic} />

## Style Type
<Canvas of={PSelectDropdownStories.StyleType} />

## Appearance Type
<Canvas of={PSelectDropdownStories.AppearanceType} />

## Disabled
<Canvas of={PSelectDropdownStories.Disabled} />

## Readonly
<Canvas of={PSelectDropdownStories.Readonly} />

## Invalid
<Canvas of={PSelectDropdownStories.Invalid} />

## Placeholder
<Canvas of={PSelectDropdownStories.Placeholder} />

## Block
<Canvas of={PSelectDropdownStories.Block} />

## Menu Width
The menuWidth prop for the select dropdown component accepts two types of values: target-width and standard CSS values. The default value is 'auto'.

By default, the dropdown menu's width is determined by the button's width, serving as the minimum width (minWidth). However, when menuWidth is set to a standard CSS value, that value is applied to minWidth, width, and maxWidth, effectively fixing the menu width to that specified value.

If the value is 'auto', the minWidth will still match the button's width, while the width and maxWidth can adjust as necessary.

When the menuWidth is set to 'target-width', the minWidth, width, and maxWidth will all be equal to the width of the dropdown button.

<Canvas of={PSelectDropdownStories.MenuWidth} />

## Selection Label
<Canvas of={PSelectDropdownStories.SelectionLabel} />

## Selection Highlight
<Canvas of={PSelectDropdownStories.SelectionHighlight} />

## Show Alert Dot
<Canvas of={PSelectDropdownStories.ShowAlertDot} />

## Show Delete All Button
<Canvas of={PSelectDropdownStories.ShowDeleteAllButton} />

## Use Fixed Menu Style
<Canvas of={PSelectDropdownStories.UseFixedMenuStyle} />

## Button Icon Type
<Canvas of={PSelectDropdownStories.ButtonIconType} />

## Custom Icon Button
<Canvas of={PSelectDropdownStories.CustomIconButton} />

## Is Fixed Width
<Canvas of={PSelectDropdownStories.IsFixedWidth} />

## Is Filterable
<Canvas of={PSelectDropdownStories.IsFilterable} />

## Show Related Header to the Selected Zone

Give ```headerName``` to the menu item to show the related header to the selected zone. <br/>
If you give the ```hideHeaderWithoutItems``` prop, the header will be hidden in the non-selected zone when there is no related items. <br/>
<Canvas of={PSelectDropdownStories.ShowRelatedHeadertotheSelectedZone} />

## Using Custom Handler & Loading

Use a handler to show the menu with the return value. <br/>
So with custom handler, there is no need to give menu prop. <br/>

You can control loading manually by loading prop. <br/>
If you don't give loading prop, loading UI will be displayed automatically when handler is running. <br/>

Handler type:

```typescript
interface HandlerRes {
    results: MenuItem[]; // this is context menu items
    totalCount?: number; // will be deprecated
    more?: boolean; // Whether to show 'show more' item on the bottom or not
}
interface AutocompleteHandler {
    (inputText: string, pageStart?: number, pageLimit?: number): Promise<HandlerRes>|HandlerRes;
}
```
<Canvas of={PSelectDropdownStories.UsingCustomHandlerAndLoading} />

## Show Select Header
<Canvas of={PSelectDropdownStories.ShowSelectHeader} />

## Show Select Marker
<Canvas of={PSelectDropdownStories.ShowSelectMarker} />

## Show Clear Selection
<Canvas of={PSelectDropdownStories.ShowClearSelection} />

## Menu Position
<Canvas of={PSelectDropdownStories.MenuPosition} />

## Disable Handler

As you enter values into the search input, the menu is filtered by default handler. <br/>
If you don't want to use this feature, give true to the disableHandler prop.
<Canvas of={PSelectDropdownStories.DisableHandler} />

## Show More & Page Size

When using the `handler` prop, the first argument of the handler is the input text, the second argument is the pageStart value, and the third argument is the pageLimit. <br/>
pageStart and pageLimit are calculated based on the value given to the `pageSize` prop. <br/>

For example, if `pageSize` is 5, pageStart is initially given 1 and pageLimit is given 5. <br/>
If the value of more in the result of executing the handler is true, the show more button is displayed. <br/>
When the user presses the show more button, the handler runs again. <br/>
At this time, 6 is given as the value of the pageStart argument and 10 is given as the value of the pageLimit argument. <br/>
<Canvas of={PSelectDropdownStories.ShowMoreAndPageSize} />

## Playground
<Canvas of={PSelectDropdownStories.Playground} />
<Controls of={PSelectDropdownStories.Playground} />

<br/>
<br/>
