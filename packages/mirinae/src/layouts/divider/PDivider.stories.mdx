import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs/blocks';
import PDivider from './PDivider.vue';
import { getDividerArgTypes } from '@/layouts/divider/story-helper';


<Meta title='Layouts/Divider' parameters={{
    design: {
        type: 'figma',
        url: 'https://www.figma.com/file/wq4wSowBcADBuUrMEZLz6i/SpaceONE-Console-Design?node-id=6980%3A163403'
    }
}} component={PDivider} argTypes={getDividerArgTypes()}
/>

export const Template = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { PDivider },
    template: `
<div style="width: 50vw; height: 50vh; border: 1px dashed #415ee1">
    <h1>The hr tags defines a thematic horizontal break in an HTML page</h1>
    <p-divider :vertical="vertical" />
    <div class="grid grid-cols-2">
        <div class="col-span-1">
            <p>split</p>
            <p-divider :vertical="vertical" style="margin-top:8px; margin-bottom:8px"/>
            <p>split</p>
        </div>
        <div class="col-span-1">
            <p>split</p>
            <p-divider :vertical="vertical" style="margin-top:8px; margin-bottom:8px"/>
            <p>split</p>
        </div>
    </div>
</div>
    `,
    setup() {
        return {}
    }
});


# Divider

<br/>
<br/>

## Playground
<Canvas>
    <Story name="Playground">
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable story="Playground"/>
