import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs/blocks';
import PSpinner from './PSpinner.vue';
import {getSpinnerArgTypes} from '@/feedbacks/loading/spinner/story-helper'; import {
    SPINNER_SIZE
} from "@/feedbacks/loading/spinner/type";
import { reactive, toRefs} from 'vue';

<Meta title='Feedbacks/Loading/Spinner' parameters={{
    design: {
        type: 'figma',
        url: 'https://www.figma.com/file/IS6P8y1Wn2nfBC4jGlSiya/Components?node-id=5687%3A372907&viewport=-33728%2C-44113%2C1.65',
    }
}} argTypes={getSpinnerArgTypes()}/>


export const Template = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { PSpinner },
    template: `
        <p-spinner
            :size="size"
            :style-type="styleType"
        ></p-spinner>
    `,
    setup() {
        return {}
    }
});


# Spinner

<br/>
<br/>

## Basic
<Canvas>
    <Story name="Basic">
        {{
            components: { PSpinner },
            template: `
                <p-spinner />
            `
        }}
    </Story>
</Canvas>

<br/>
<br/>

## Style Type

<Canvas>
    <Story name="Style Type">
        {{
            components: { PSpinner },
            template: `
                <div class="flex items-center">
                    <span class="mr-4"><p-spinner style-type="gray"/></span>
                    <span class="bg-black"><p-spinner style-type="white"/></span>
                </div>
            `
        }}
    </Story>
</Canvas>

<br/>
<br/>

## Size
<Canvas>
    <Story name="Size">
        {{
            components: { PSpinner },
            template: `
                <div class="flex items-center">
                    <p-spinner v-for="size in sizes" class="mr-4" :size="size" />
                </div>
            `,
            setup() {
                const state = reactive({
                    sizes: Object.values(SPINNER_SIZE)
                })
                return {
                    ...toRefs(state)
                }
            }
        }}
    </Story>
</Canvas>

<br/>
<br/>

## Playground

<Canvas>
    <Story name="Playground">
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable story="Playground"/>

