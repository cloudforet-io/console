import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs/blocks';
import PTableCheckModal from './PTableCheckModal.vue';
import PButton from '@/inputs/buttons/button/PButton.vue';
import { ref } from 'vue'; import {
    getTableCheckModalArgTypes
} from '@/feedbacks/modals/advanced/table-check-modal/story-helper';


<Meta title='Feedbacks/Modals/Advanced/Table Check Modal' parameters={{
    design: {
        type: 'figma',
        url: 'https://www.figma.com/file/t0napBIB14ZZN9RIq9uo3O/Asset-Inventory?node-id=2613%3A371966&t=0A8wuvLdPqlyb3TM-4'
    }
}} component={PTableCheckModal} argTypes={getTableCheckModalArgTypes()}
/>

export const Template = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { PTableCheckModal, PButton },
    template: `
        <div>
            <p-button @click="click">Open Modal</p-button>
            <p-table-check-modal
                :modal-size="modalSize"
                :theme-color="themeColor"
                :header-title="headerTitle"
                :sub-title="subTitle"
                :fields="fields"
                :items="items"
                :visible.sync="visible"
                :loading="loading"
                @cancel="close"
                @close="close"
            />
        </div>
    `,
    setup() {
        const visible = ref(false);
        return {
            visible,
            click() {
                visible.value = true;
            },
            close() {
                visible.value = false;
            },
        }
    }
});


# Table Check Modal

<br/>
<br/>

<Canvas>
    <Story name="Playground">
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable story="Playground"/>
