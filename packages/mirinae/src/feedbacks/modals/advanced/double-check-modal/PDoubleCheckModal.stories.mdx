import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs/blocks';
import PDoubleCheckModal from './PDoubleCheckModal.vue';
import PButton from '@/inputs/buttons/button/PButton.vue';
import {
    getDoubleCheckModalArgTypes
} from '@/feedbacks/modals/advanced/double-check-modal/story-helper';
import { ref } from 'vue';


<Meta title='Feedbacks/Modals/Advanced/Double Check Modal' parameters={{
    design: {
        type: 'figma',
        url: 'https://www.figma.com/file/t0napBIB14ZZN9RIq9uo3O/Asset-Inventory?node-id=2613%3A371966&t=0A8wuvLdPqlyb3TM-4'
    }
}} component={PDoubleCheckModal} argTypes={getDoubleCheckModalArgTypes()}
/>

export const Template = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { PDoubleCheckModal, PButton },
    template: `
        <div>
            <p-button @click="click">Open Modal</p-button>
            <p-double-check-modal
                :modal-size="modalSize"
                :verification-text="verificationText"
                :header-title="headerTitle"
                :visible.sync="visible"
                @cancel="close"
                @close="close"
            />
        </div>
    `,
    setup() {
        const visible = ref(false);
        return {
            visible,
            click() {
                visible.value = true;
            },
            close() {
                visible.value = false;
            },
        }
    }
});


# Double Check Modal

<br/>
<br/>

<Canvas>
    <Story name="Playground">
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable story="Playground"/>
