import { green, indigo, violet } from '@/styles/colors';

export const repositoryIconMap = {
    local: 'ic_private-repository',
    managed: 'ic_badge-with-ribbon',
    remote: 'ic_marketplace',
};

export const repositoryColorMap = {
    local: green[700],
    managed: violet[600],
    remote: indigo[600],
};

export const repositoryBackgroundColorMap = {
    local: green[100],
    managed: violet[100],
    remote: indigo[100],
};
